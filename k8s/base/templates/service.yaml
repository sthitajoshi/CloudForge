apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudforge-app.fullname" . }}
  labels:
    {{- include "cloudforge-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudforge-app.selectorLabels" . | nindent 4 }}
